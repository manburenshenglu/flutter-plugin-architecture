name: module_home
description: Home business module with configurable entry presentation.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  get: ^4.7.2
  get_it: ^8.2.0
  foundation:
    path: ../../foundation
  design_system:
    path: ../../design_system
  shared:
    path: ../../shared

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
